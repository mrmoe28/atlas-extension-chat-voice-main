{
  "name": "atlas-voice-extension",
  "private": true,
  "version": "1.0.0",
  "description": "Atlas Voice Chrome Extension with direct OpenAI connection",
  "scripts": {
    "test": "playwright test"
  },
  "dependencies": {
    "@vercel/postgres": "^0.5.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "playwright": "^1.56.1"
  }
}